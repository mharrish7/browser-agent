# Instructions
  - Install the requirements with `pip install requirements.txt`
  - Create a file called `config.py` and add gemini api key (refer config-example) (You can get it for free!)
  - run  `python browser_agent.py`
